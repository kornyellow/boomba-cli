#pragma once

// Includes

class Position {
public:

    unsigned long int x;
    unsigned long int y;
    Position(unsigned long int x, unsigned long int y) {

        this->x = x;
        this->y = y; 
    }
};